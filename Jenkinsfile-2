pipeline {
    agent any

    stages {
        stage ('Deploy') {
            steps {
                script {
                    sshagent(credentials : ['key']) {
                       sh '''
                            scp  index.html ec2-user@ec2-18-217-189-22.us-east-2.compute.amazonaws.com://usr/share/nginx/html/index.html
                       '''
                    }
                }
            }
        }
    }
}